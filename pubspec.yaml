name: azuread_login_view
description: Provides one or more widgets to handle OAuth logins via Azure Active Directory.
version: 0.0.5
homepage: https://github.com/egomobile/azuread_login_view

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: 0.13.4
  webview_flutter: 3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
